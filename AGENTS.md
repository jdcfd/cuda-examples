# AI Agent Instructions

To ensure consistency in this project, please follow these guidelines:

- **Python**: Use `pixi` for environment management, running scripts, and adding dependencies.
- **C++ / CUDA**: Use `cmake` for building and managing these projects. Do NOT use `pixi` for C++/CUDA.
